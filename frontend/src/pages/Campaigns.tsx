// TODO: Implement campaigns page (mirrors Donors pattern)
// Endpoints: GET /api/v1/campaigns

export default function Campaigns() {
  return (
    <div style={{ padding: '0' }}>
      <h1 style={{ fontSize: '26px', fontWeight: 700, color: 'var(--text)', marginBottom: '8px' }}>Campaigns</h1>
      <p style={{ color: 'var(--text-muted)' }}>Campaign management — coming soon.</p>
    </div>
  );
}
